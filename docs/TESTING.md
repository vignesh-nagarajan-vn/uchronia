# Testing

## How to run

```sh
pnpm test          # vitest across all packages (~281 tests)
pnpm typecheck     # tsc --noEmit everywhere
pnpm lint          # biome check
pnpm e2e           # playwright mock-mode journey (boots both servers keyless)
pnpm verify:vercel # build the serverless bundle, stage it like Vercel ships it, smoke it
```

Per package: `pnpm --filter @uchronia/<name> test` (`test:watch`, `test:coverage`).

## Demo (mock) mode

`UCHRONIA_MOCK=1` (or simply no API key) swaps in the deterministic `MockProvider` - surfaced to users as **demo mode** (v2/M13) - seeded per request from template id + version + seedKey, so identical inputs produce identical fixtures. Arbitrary PODs work end to end. All integration tests, the journey, and CI run keyless, forever (v2 ground rule).

Every template in the registry has a demo handler, the v2/M17 ones included, so symposium derivation and the Court of Plausibility are exercisable without a key. v2/M18 needed no new handler, only honest ones: the mock era walks the regional dials a few points at a time, always inside the validator's bound, and reads the previous value back out of the pre-rendered index summary so the demo's dials continue across eras instead of resetting (a mock that reset them would never exercise rule 12's arithmetic check at all); name drift fires every third era against a real event in the batch; and the mock convergence scan names an attractor and, when the years genuinely differ, a path note, while leaving lateness to the pipeline's arithmetic where it belongs.

Two details of the mock symposium exist to keep it an honest test subject rather than a convenient one. Each chair's draft starts at a different offset in the era, so the three drafts genuinely differ instead of being the same head slice three times over. And the synthesizer folds duplicate tellings together by title, then takes an **even stride across the whole chronological run** rather than draining the pools from the front: front-draining would leave the era's later years empty and would quietly hide its shakiest material (which the chairs put last) from the critic, and with it from the court.

## Test matrix (§11.3, implemented)

- **schemas** (14): round-trips, fixture parsing, draft/llm shapes, artifact discrimination.
- **core unit** (176 with integration): seeded rng · dial mapping (bands, budgets, floors) · store: change-log replay, multi-level fork resolution, mutation guards incl. pre-fork immutability · every validator rule individually, including `no-posthumous-mutation` with its branch-locality · region-aware `anchorsNear` ranking · state-snapshot budgeting caps · structured repair loop (success/repair/fail-loud/non-JSON) · mock pod intake determinism · era planning.
- **core unit, v2/M14 additions**: `pod-sketch` (the year-2 bug regression suite, alias spellings, era markers, 4-over-3-digit preference, garbage yields null not dice) · retrieval (tokenization, on-subject ranking, year bias, deterministic tie-breaks) · mock `pod-interpret` (demo WW2 gate at unit level, clarifying-question rules, determinism) · `batchReachesPod` (connected chains, free-floating drift, order-independent relaxation, hop bound) · critic on-divergence verdicts carried to visible disputes (injected critic handler).
- **core unit, v2/M17 additions**: `dial.test.ts` axis cases (every axis derived from the master dial, explicit axes taken verbatim, volatile technology and chaos each widening the wildcard envelope at distance, `axesLanguage` silent at the defaults and speaking when an axis moves, and the case where a reader silences shocks a butterfly dial would have brought) · `pipeline/symposium.test.ts` (`chooseSpecialists` seats the chairs the pressures call for, lends weight to the military chair when a pressure presses hard, never seats a chair twice, and still fills the bench with no pressures at all; plus symposium derivation end to end in mock, marking what the chairs could not settle and leaving standard derivation with no contested marks at all) · `pipeline/court.test.ts` pins each of the three rulings by injecting a `court-judge` mock handler (uphold clears the dispute, dispute keeps the mark and the transcript, revise orders one retelling), and asserts the court never sits when it is switched off.
- **core unit, v2/M18 additions**: `src/lives.test.ts` covers the pure pieces without a provider (role tenures replayed onto a fixture world: a role opens a span and the next closes it, a terminal delta closes whatever is open, an entity that never held an office reports none · deep time: `defaultHorizonYears` carries 1453 to the present, floors a divergence inside living memory at 60 years and clamps at 6000; `planEraSpans` keeps widening so six thousand years costs fewer than twenty eras while a short horizon is exactly as it was · rule 12 directly: an honest walk passes, a claim reporting delta 2 while moving 30 fails both counts, and a 30-point fall passes only when its note names the plague). `src/pipeline/claims.test.ts` runs the mock pipeline end to end (index readings and name drift recorded against committed events and streamed as `claim.recorded`, every claim hanging off an event the branch can see, readings inside 0-100, `indexContinuity` and the whole validator finding nothing on a clean run, `summarizeIndices` carrying the latest reading per region and saying nothing at all on a branch with no claims, drifts glossing names only, **a fork not inheriting claims made after its cut**, and the epilogue adding exactly one speculative era at horizon + 50 years with none at all when it is switched off).
- **core integration**: full pipeline in mock (seed + era loop to the horizon, determinism, resume-after-interrupt), dual-review paths (regenerate-and-fix, dispute-kept, machine-drop, wildcard floor), convergence against the real baseline, cooperative abort mid-run (committed prefix stays valid), a failing convergence scan degrading to a warning, fork discipline (a late fork's first era requests batch 4, wildcards 0, asserted via a spy provider), lazy expansion fill-once semantics, multi-level fork generation with sub-PODs.
- **server** (76): routes with the injected MockProvider: CRUD, import/export round-trip **including referential 422 rejection**, branch views through forks, SSE lifecycle including client abort mid-stream, **concurrent-run 409**, **partial-era healing on resume**, PATCH rename/dial/horizon (shrink refused), regenerate-in-place (identity kept, inherited events refused), leaf-branch deletion (roots and parents refused), expansion persistence, fork/compare, artifacts, md/html exporters, **config parsing** (empty-vars-mean-unset, serverless `/tmp` redirect, host binding, the structured-outputs-capable generation default), **malformed-JSON 400 envelope**, plus **AnthropicProvider unit tests** via an injectable stub client (usage mapping, truncation retry with doubled budget, refusal, abort-signal passthrough, role→model routing).
- **server, v2/M17** (`src/symposium.test.ts`): the derivation settings the composer sent survive the round trip, absent ones default to standard with no court, a symposium run marks contested events, court transcripts persist and stream and export, and an exported chronicle carrying court records re-imports.
- **web** (13): year formatting · thread geometry math (sag, symmetry, caps) · EventCard/Stamp component tests (jsdom) incl. disputed/convergence marks and off-screen relation counts.
- **e2e** (Playwright, chromium, 5 specs): the §11.3 journey: gallery POD (v2/M16: one click composes, the interpretation card comes up reading "from the catalogue", and the reader confirms it) → SSE stream to completion → dual-review marks walked into view with `j` → ledger search → open event → expand → generate artifact → read it → fork with sub-POD → child derivation → delta → export JSON download. Plus the ephemerality dead end: a vanished branch renders the honest explanation with a working way back to the atlas, not a retry loop. Plus demo-mode honesty (M13): the DEMO pill and composer banner are visible, the pill walks to Settings, and the live-connection check answers with the demo truth. Plus the demo WW2 gate (M14): "What if the Allies lost World War 2" through the interpretation card lands in 1939-1945 with the real candidate mechanisms, a chip applies its year, and the ledger derives from the confirmed reading. Plus the M17 spec, "the symposium sits, the court rules, and the axes come off the master dial": the axes flyout opens, technology is taken off the master dial and handed back, symposium and court are switched on and the composer says what each does, a derivation runs to completion, the branch view is read back over HTTP to confirm at least one contested event and at least one court record, the contested mark is walked into view in the ledger, and the tried event's detail page shows the court transcript with its ruling.

  The journey's server now boots on an **in-memory database** (`start:e2e` sets `UCHRONIA_DB=:memory:`, and `reuseExistingServer: false` in `apps/web/playwright.config.ts` guarantees a fresh one). With a file database, chronicles from a previous run lingered in the catalogue and collided with the journey's own locators: the walk reaches for the first match of several of them, so the first match has to belong to this run rather than to yesterday's.
- **fake-Vercel smoke** (`scripts/verify-vercel.mjs`, run by `pnpm verify:vercel`): stages `api/index.mjs` + `apps/server/dist/**` exactly as Vercel ships the function, imports the bundle under plain Node with `VERCEL=1`, and asserts cold start (migrations at the staged path, bundled-ledger seeding, the native sqlite binding), a branch view, the font-embedded HTML export, and the 404 envelope.

## Machine validator rules (each unit-tested)

dates-monotonic · event-within-era · edge-endpoints-exist · entities-exist · deltas-apply · no-posthumous-mutation · plausibility-range · era-overlap · fork-normalized (+ store-level pre-fork immutability guards) · tech-prerequisite (v2/M15: DAG-resolved absurdity floors; accelerated-but-plausible tech passes) · demographic-plausibility (v2/M15: no person active past a 110-year span) · index-continuity (v2/M18, rule 12: a regional index must report its own arithmetic, and a move past `MAX_INDEX_DELTA` needs a catastrophe or a boom named in the note; the size check judges the actual movement, not the reported delta). Advisory (warns, never drops): geographic-plausibility (keyword-inferred far-theatre jumps under a year, pre-telegraph).

## Fuzzing (v2/M15, fast-check, runs in `pnpm test`)

Demo intake never throws and always yields schema-valid output over arbitrary unicode (`core/src/intake.fuzz.test.ts`); arbitrary JSON documents and malformed bodies through import/create answer 4xx envelopes with no partial writes (`apps/server/src/import.fuzz.test.ts`).

## Evals

See [EVALS.md](EVALS.md): the 31-POD benchmark, the CI mock lane (also asserted inside `pnpm test` by the 2 specs in `bench.test.ts`), the judged live lane, the critic A/B, and the release thresholds (the WW2 gate).

## CI

GitHub Actions on push/PR to main, all in mock mode, no secrets:

- **secrets** (ubuntu): `node scripts/check-secrets.mjs` - the tree and staged diff scanned for key material with matches redacted, and the `.env` ignore verified.
- **checks** - a 2×2 matrix (ubuntu/windows × node 22/24): pnpm install → biome → typecheck → vitest → **pnpm eval** (the mock lane: 31-POD intake benchmark, keyless) → build.
- **vercel-shape** (ubuntu, node 22): `pnpm verify:vercel` (the fake-Vercel smoke above) plus the web build and root-`dist` mirror - the exact steps `vercel.json` runs, on the OS Vercel builds on.
- **journey** (ubuntu): the Playwright chromium journey; traces upload as artifacts on failure.
