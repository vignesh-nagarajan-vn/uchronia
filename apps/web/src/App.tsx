export function App() {
  // Placeholder shell. The RED THREAD interface lands at M9 — no UI is built
  // before docs/DESIGN.md is finalized (§7.8).
  return (
    <main>
      <h1>Uchronia</h1>
      <p>A blank ledger. Choose a moment to diverge.</p>
    </main>
  )
}
