# dependencies
node_modules/

# builds
dist/
*.tsbuildinfo

# environment - secrets never committed
.env
.env.local

# the v2 master prompt lives outside the repo; its expectations live in docs/
/V2_MASTER_PROMPT.md

# runtime data (SQLite lives under apps/server/data; packages/core/data is source!)
apps/server/data/
*.db
*.db-journal
*.db-wal
*.db-shm

# test output
coverage/
playwright-report/
test-results/
blob-report/

# editor / os
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# vercel project link (local) + the verify:vercel staging area
.vercel/
apps/server/.vercel-stage/
